### Web 版本

本项目已提供纯前端版本的奶量计算器，无需后端服务即可使用。

#### 使用方式
1. 进入 `web/` 目录。
2. 使用任意静态服务器打开（例如：`python -m http.server`），或直接双击 `index.html`。
3. 在页面中选择职业并填写参数，结果会自动实时计算。

#### 说明
- 计算逻辑已完整迁移到前端 JavaScript。
- “设为基准”用于记录当前结果并显示差值。
- “设为被动基准”用于锁定被动技能基准等级。
